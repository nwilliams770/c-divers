%edi, %esi, and %ebx are callee-save meaning that the procedure must save them on the stack before altering their values and restore them before returning. The other registers are caller-save meaning that they can be altered without affecting the behavior of the caller.
