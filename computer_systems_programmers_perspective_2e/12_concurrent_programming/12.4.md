Reinitializing `ready_set` before each `select` call ensures that it accurately represents the set of descriptors ready for reading that specific moment, avoiding issues with closed or invalid connections.
