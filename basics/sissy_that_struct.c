struct point {
  int x;
  int y;
};
// To avoid having to use 'struct point' every time we want a new point
// We can use typedef

typedef struct point {
  int x;
  int y;
} point;

void main() {
  // Struct: basic foundation for objects and classes
}
